# URL of the Django backend. Leave empty in local dev — the Vite proxy
# (see vite.config.js) forwards /api/* to http://127.0.0.1:8000.
# In production (e.g. Vercel), set VITE_API_BASE=https://your-backend.onrender.com
VITE_API_BASE=
